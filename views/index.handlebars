{{!-- Setup the index.handlebars to have the template that Handlebars can render onto.

Create a button in index.handlebars that will submit the user input into the database. --}}

{{!-- Add the Burger image --}}

<h1>Eat-Da-Burger!</h1>
<div class="container">
  <div class="row">
    <div class="col-sm-6">
      <ul class="list-group">

        {{#each object}}
        <button type="button" class="btn btn-secondary" style="float: right;">Devour it!</button>
        <li class="list-group-item">{{"id"}}. {{"burger_name"}}</li>

        {{/each}}
      </ul>
    </div>
    <div class="col-sm-6">
      <ul class="list-group">

        {{#each object}}

        <li class="list-group-item">{{"burger_name"}}
        </li>

        {{/each}}
      </ul>
    </div>
  </div>

  <div class="row">
    <div class="form-group">
      <label for="exampleFormControlTextarea1"></label>
      <textarea class="form-control" id="exampleFormControlTextarea1" rows="3"></textarea>
    </div>
    <input class="btn btn-primary" type="submit" value="Submit">

    <p>Enter a burger you want to eat; Click the "Submit" button</p>


  </div>
</div>

{{!-- if devoured is false
render burger name to the left
if devoured is true
render burger name to the left --}}

{{!-- Burger name not eaten | Devour it button | Burgers eaten --}}

{{!-- Text box --}}